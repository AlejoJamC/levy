"""
levy.dashboard -- testable core for the results dashboard (LEV-10 / D6).

Bundle loading/validation (`bundle.py`), curve selection (`curves.py`), and
the live query decision (`query.py`). No module here imports the UI
framework; the Streamlit shell lives at `scripts/dashboard.py`.
"""
