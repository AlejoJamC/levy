name: levy
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - numpy>=1.24.0
  - httpx>=0.24.0
  - python-dotenv>=1.0.0
  - redis>=5.0.0
  - faiss-cpu>=1.7  # install via conda-forge (pip wheel segfaults on Apple Silicon arm64)
  - pip:
    - sentence-transformers>=3.0
    - transformers>=4.48
